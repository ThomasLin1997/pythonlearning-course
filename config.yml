title: pythonlearning
description: My first github about python learning
template:
  repo: pythonlearning-template
  name: pythonlearning
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
